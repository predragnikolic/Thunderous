import { component, css } from 'thunderous';

const myStyleSheet = css`
  :host {
    display: block;
    font-family: sans-serif;
  }
`;

const MyElement = component(({ adoptStyleSheet }) => {

  adoptStyleSheet(myStyleSheet);

  // ...
});
