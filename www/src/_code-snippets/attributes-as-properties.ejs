const MyElement = component<{ count: number }>(({ props }) => {
  const count = props.count.init(0);

  // count.set() will update both the DOM property, AND the HTML attribute.

  return html`<output>${count}</output>`;
}, { attributesAsProperties: [['count', Number]] });
