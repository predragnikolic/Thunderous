const count = signal(0, {
  debugMode: true,
  label: 'count signal',
});

count.set(1, {
  debugMode: true,
  label: 'start count',
});

const newCount = count({
  debugMode: true,
  label: 'new count',
});
