import { derived, signal } from 'thunderous';

const [count, setCount] = signal(0);

const timesTen = derived(() => count() * 10);

console.log(timesTen()); // 0

setCount(10);

console.log(timesTen()); // 100
