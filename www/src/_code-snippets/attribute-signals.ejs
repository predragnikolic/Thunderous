const MyElement = component(({ props }) => {
  const heading = props['my-heading'];

  // heading.set() will also update the `my-heading` attribute in the HTML.

  return html`<h2>${heading}</h2>`;
}, {
  props: ['my-heading', String]
});
